# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    user_id:
        label: keerill.users::lang.user.label
        relation: user
        select: name
    
    ip_address:
        label: keerill.users::lang.user.ip_address

    message:
        label: keerill.users::lang.logs.message

    is_success:
        label: keerill.users::lang.accessLogs.status

    created_at:
        label: keerill.users::lang.logs.created_at
        type: datetime
    
