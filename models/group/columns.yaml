# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    name:
        label: keerill.users::lang.group.name

    code:
        label: keerill.users::lang.group.code
