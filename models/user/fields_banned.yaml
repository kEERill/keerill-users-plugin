# ===================================
#  Form Field Definitions
# ===================================

fields:
  is_banned:
    label: keerill.users::lang.user.is_banned
    comment: keerill.users::lang.user.is_banned_desc
    type: checkbox

  is_banned_reason:
    label: keerill.users::lang.user.is_banned_reason
    commentAbove: keerill.users::lang.user.is_banned_reason_desc
    type: textarea