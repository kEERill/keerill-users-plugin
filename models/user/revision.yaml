columns:
    id:
        label: ID

    field:
        label: keerill.users::lang.user.revision.field

    old_value:
        label: keerill.users::lang.user.revision.old_value

    new_value:
        label: keerill.users::lang.user.revision.new_value

    updated_at:
        label: keerill.users::lang.logs.created_at
        type: datetime
        format: j M Y H:i