# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID

    name:
        label: keerill.users::lang.user.name
        searchable: true
        
    ip_address:
        label: keerill.users::lang.user.ip_address

    email:
        label: keerill.users::lang.user.email
        searchable: true

    group_id:
        label: keerill.users::lang.user.group
        relation: group
        select: name
        searchable: true

    last_seen:
        label: keerill.users::lang.user.last_seen
        type: datetime
        format: j M Y H:i

    created_at:
        label: keerill.users::lang.user.created_at
        type: datetime
        format: j M Y H:i
