# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    user_id:
        label: keerill.users::lang.user.label
        relation: user
        select: name
        searchable: true

    ip_address:
        label: keerill.users::lang.user.ip_address
        searchable: true
    
    message:
        label: keerill.users::lang.logs.message
    
    code:
        label: keerill.users::lang.logs.code
        searchable: true

    created_at:
        label: keerill.users::lang.logs.created_at
        type: datetime